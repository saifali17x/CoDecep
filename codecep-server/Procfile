# NOT THE ACTIVE PROCFILE. The app deploys from the repo ROOT, and Heroku only
# reads the Procfile at the root of what it builds — so this file is inert.
#
# It is kept as documentation of the server's own process types for the
# alternative deploy shape (pushing codecep-server/ alone, via a subdirectory
# buildpack or `git subtree push`), where it WOULD be the one Heroku reads.
#
# The live one is ../Procfile. Change that, not this. See CLAUDE.md §7.11.
web: npm start
release: npx prisma migrate deploy
